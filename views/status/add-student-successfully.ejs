<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>تم إضافة الطالب بنجاح</title>
  <link rel="stylesheet" href="../public/css/main.css">
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      background-color: #111827;
      font-family: 'Cairo', sans-serif;
      color: white;
      padding: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .profile-container {
      max-width: 800px;
      background-color: #2d3748;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 0 15px rgba(0,0,0,0.2);
      margin: auto;
      display: flex;
      flex-direction: column;
      align-items: center;
      width: 100%;
    }

    .profile-container h2 {
      color: #fbbf24;
      font-size: 32px;
      margin-bottom: 20px;
      text-align: center;
    }

    .profile-container .info-section {
      width: 100%;
      margin-bottom: 30px;
    }

    .info-section h3 {
      font-size: 24px;
      color: #facc15;
      margin-bottom: 15px;
      text-align: center;
    }

    .info-section p {
      font-size: 18px;
      color: #e5e7eb;
      margin-bottom: 10px;
      display: flex;
      justify-content: space-between;
      padding: 8px;
      border-bottom: 1px solid #374151;
    }

    .info-section p span {
      color: #fbbf24;
    }

    .code-section {
      background-color: #374151;
      padding: 12px;
      border-radius: 8px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 20px;
      width: 100%;
    }

    .code-section span {
      font-size: 18px;
      color: #fbbf24;
    }

    .code-section button {
      background-color: #fbbf24;
      color: black;
      padding: 10px 15px;
      font-size: 16px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: 0.3s;
    }

    .code-section button:hover {
      background-color: #f59e0b;
    }

    .code-section .copy-space {
      margin-left: 15px;
    }

    .action-buttons {
      margin-top: 30px;
      display: flex;
      gap: 20px;
      width: 100%;
      justify-content: center;
    }

    .action-buttons button {
      background-color: #374151;
      color: #fbbf24;
      padding: 12px 20px;
      font-size: 18px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: 0.3s;
    }

    .action-buttons button:hover {
      background-color: #f59e0b;
    }

    @media (max-width: 768px) {
      .profile-container {
        padding: 30px;
      }

      .profile-container h2 {
        font-size: 26px;
      }

      .info-section p {
        font-size: 16px;
      }

      .action-buttons button {
        font-size: 16px;
        padding: 10px 18px;
      }
    }
  </style>
</head>
<body>

  <div class="profile-container">
    <h2>✅ تم إضافة الطالب <%= student.studentName %> بنجاح!</h2>

    <!-- معلومات الطالب -->
    <div class="info-section">
      <h3>📚 معلومات الطالب</h3>
      <p><span>الاسم:</span> <%= student.studentName %></p>
      <p><span>رقم الطالب:</span> <%= student.studentNumber %></p>
      <p><span>المرحلة الدراسية:</span> <%= student.studentGrade %></p>
    </div>

    <!-- قسم الكود -->
    <div class="code-section">
      <span>🔑 الكود الخاص بك:</span>
      <span id="studentCode"><%= student._id %></span>
      <div class="copy-space">
        <button onclick="copyCode()">🔗 نسخ الكود</button>
      </div>
    </div>

    <!-- أزرار إضافية -->
    <div class="action-buttons">
      <button onclick="window.location.href='<%= baseUrl %><%= url %>'">➕ إضافة طالب آخر</button>
      <button onclick="window.location.href='/'">🏠 الرجوع للصفحة الرئيسية</button>
    </div>

  </div>

  <script>
    function copyCode() {
      // تحديد العنصر الذي يحتوي على الكود
      var code = document.getElementById("studentCode");

      // إنشاء نطاق النص
      var range = document.createRange();
      range.selectNode(code);

      // تحديد النص في نطاق النص
      window.getSelection().removeAllRanges(); 
      window.getSelection().addRange(range);

      // نسخ النص
      document.execCommand("copy");

      // تنبيه المستخدم
      alert("تم نسخ الكود بنجاح! ✅");
    }
  </script>

</body>
</html>
